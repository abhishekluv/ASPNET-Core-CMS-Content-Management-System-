@model Sidebar

<div class="card">
    <div class="card-body">
        <h5 class="card-title">@Model.SidebarTitle</h5>
        <p class="card-text">
            @Html.Raw(Model.Content)
        </p>
    </div>
</div>