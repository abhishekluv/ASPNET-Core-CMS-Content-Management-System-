@model List<Page>

@foreach(var item in Model)
{
    <li class="nav-item">
        <a class="nav-link text-dark" asp-controller="Home" asp-action="Index" asp-route-slug="@item.Slug" title="@item.Title">@item.Title</a>
    </li>
}

